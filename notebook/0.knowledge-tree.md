# AI Learning System — 知识树

> 这是你的个人知识图谱。每一个节点不是"学完了"，而是"正在生长中"。
>
> 标记说明：
> - 🌱 已建立节点，待深入
> - 🌿 正在深挖中
> - 🌳 已形成系统理解
> - ⭐ 核心研究方向

---

## 根：AI Learning System

```text
AI Learning System
│
├── Mathematics
├── Data              ← 你的核心壁垒
├── Learning
├── Decision
├── Evaluation
└── System
```

---

## 一、Mathematics（数学基础）

```text
Mathematics
│
├── Linear Algebra 🌱
├── Probability & Statistics 🌱
├── Information Theory 🌿
│   ├── Entropy
│   ├── Mutual Information
│   ├── KL Divergence
│   └── Information Bottleneck 🌿 ⭐ (CoT 压缩的数学本质)
├── Optimization 🌱
│   ├── Gradient Descent
│   ├── SGD / Adam
│   └── Convergence Theory
└── Graph Theory 🌱
```

---

## 二、Data（数据）⭐ 核心主线

### 2.1 Foundational Data（基础数据）

```text
Foundational Data
│
├── Data Source 🌿
│   ├── Crowdsourcing
│   ├── Expert Annotation
│   ├── Web Crawling
│   ├── Synthetic Generation
│   └── User Behavior Logs
│
├── Collection 🌿
│   ├── Pipeline Design
│   ├── API Integration
│   └── Streaming vs Batch
│
├── Sampling 🌿
│   ├── Uniform Sampling
│   ├── Stratified Sampling
│   ├── Importance Sampling
│   ├── Active Sampling
│   ├── Curriculum Sampling
│   ├── Data Mixture
│   └── Scaling Law
│
├── Cleaning 🌿
│   ├── Deduplication
│   ├── Noise Detection
│   ├── Outlier Removal
│   ├── Format Normalization
│   └── LLM Repair (LLM 驱动的数据修复)
│
├── Validation 🌿
│   ├── Format Validation (Gate 1: 格式完整性)
│   ├── Schema Validation (Gate 2: 字段合规)
│   ├── Constraint Validation
│   ├── Semantic Validation (Gate 3: 内容一致性)
│   ├── Consistency Check
│   ├── Statistical Validation
│   ├── Distribution Check (Gate 5: 黑盒配比)
│   ├── Quality Gate System (Cascading Classification) 🌿
│   └── Learnability Check ⭐
│
├── Transformation 🌿
│   ├── Schema Mapping
│   ├── Format Conversion
│   ├── Label Correction (Rule + LLM 两步漏斗)
│   ├── Feature Engineering
│   └── Tokenization
│
├── Versioning 🌱
│   ├── Dataset Lineage
│   ├── Diff Tracking
│   └── Rollback Strategy
│
├── Storage 🌱
│   ├── Parquet / Arrow
│   ├── Data Lake
│   └── Indexing
│
├── Serving 🌱
│   ├── Data Loader
│   ├── Streaming Serving
│   └── Caching
│
├── Learnability ⭐ 🌿  ← 核心方向
    ├── Learning Signal (CoT 压缩 = 信号提纯)
    ├── State Transition
    ├── Information Bottleneck 🌿
    ├── Compressed CoT 🌿 (5497 条质检实践)
    ├── Data Efficiency
    ├── Curriculum Design
    └── Sample Difficulty
```

### 2.2 Evaluation（评测与洞察）

```text
Evaluation ⭐ ← 职业主线（2026-08-23 定位：评测能力四层模型 + 评测×数据交叉点，
│   见 notebook/AI策略产品与评测能力地图.md）
│
├── Judge Model 🌿 (LLM Judge 可靠性解剖完成：位置/冗长/自我增强偏差
│   + Deterministic Floor 兜底 + Cohen's Kappa 人机一致验收)
├── Reward Model 🌿 (八层解剖完成：BT 数学内核 + RLHF/DPO/GRPO/RLVR
│   方案对比 + reward hacking 案例库：CoastRunners→sycophancy→
│   Anthropic 2025 跨域泛化；能写成规则的绝不学函数)
├── Benchmark Design 🌿 (Eval Set 建设实践中)
│   ├── Train/Test Split 策略
│   ├── Black-box / White-box 配比 (20%/80%)
│   ├── 长尾知识 + Reasoning 覆盖
│   ├── 搭桥理论（业务指标 ↔ 桥梁指标 ↔ 能力指标）
│   ├── 人人一致/人机一致 🌿 (美团图灵：独裁者 + Rubric 二元化)
│   ├── Good/Bad Case 驱动数据飞轮（采集-清洗-评测-质检-归因）
│   ├── 长程 Agent 评测（行为评测：结果/过程/效率/风险四层）
│   ├── 三层评测 🌱 (平台断言 / 入口 query / 编排任务级，百度反刍：
│   │   92% 只是平台层指标，Agent 指标 = 端到端任务成功率)
│   ├── 真实任务评测集 🌿 (RealReplicaBench/E-Bench/VibeLifeBench：稀缺=蓝海，
│   │   全有或全无评分 vs 部分得分，Avg@3 vs 3 次全对)
│   ├── 评测规范设计 🌿 (39-sheet 判分规范解剖：5 部件框架 =
│   │   任务对象/证据边界/输出格式/容错规则/排除项；
│   │   判分形态：Deterministic 0/1 + 三类容差 + 部分分权重 +
│   │   mean best-position similarity；规范与 judge 代码模板一体生产)
│   ├── VLM 评测基准地图 🌿 (MMMU/MME/Video-MME/MMBench 解剖完成：
│   │   四种设计哲学 = 学科考试/能力普查/视频综合/维度矩阵；
│   │   Video-MME 字幕轨 = 证据边界；MMMU 饱和 → MMMU-Pro =
│   │   基准生命周期；MMBench 规则→LLM fallback 判分管线)
│   └── SWE-bench 🌿 (八层解剖完成：三阶段构建管线 + FAIL_TO_PASS/PASS_TO_PASS
│       判分 + Verified 污染失效 + Pro 抗污染设计 + 框架效应 11.6pp)
├── Insight & Analytics 🌿 (SQL + 数据分析 = 基础能力)
├── A/B Testing 🌿 ← 策略 PM 核心技能，与 AI Data 高度通用
│   ├── 实验设计（实验组 vs 对照组）
│   ├── 样本量计算（统计功效）
│   ├── 假设检验（t-test, chi-square）
│   ├── 指标设计（核心指标 + 护栏指标）
│   └── 因果推断（进阶）
└── Causal Inference 🌱
```

### 2.3 Frontier Data（前沿数据）

```text
Frontier Data
│
├── RL Data 🌿 (评测→训练闭环解剖完成)
│   ├── Preference Data
│   ├── Trajectory Data (SWE-smith 26k 轨迹：Runtime→训练器需桥接层)
│   ├── Reward Signal (reward function = 评测标准可执行化)
│   └── 合成数据管线 (SWE-smith：环境自动化→bug 合成→测试验证→轨迹生成)
├── Synthetic Data 🌿 (数据构造 ⊃ 合成；四谱系=模型/程序规则/仿真渲染/环境交互；
│   命门=verifier；五风险=幻觉/分布失真/模型坍缩/多样性不足/反捷径泄漏；
│   见 notebook/AI 数据策略 PM 工作闭环)
├── Curriculum Data 🌱
├── Multi-modal Data 🌿 ← 新业务落地（VLM 视频数据管线）
│   ├── Visual Reasoning 🌿 ← 六类视频能力：空间感知/计数/行为理解/
│   │   时间交叉引用/实体匹配/功能步骤对齐（= VLM 全部前沿模型短板）
│   ├── Video Data Pipeline 🌿
│   │   ├── 考点先行寻源 🌱 ← 题型 → 检索逻辑 → 难度对齐示例（采集 = 第一道难度防线，2026-08-31）
│   │   └── 难度坍缩（Difficulty Collapse）🌱 ← 空间/时间坍缩翻车案例；难度 = 训练信号的一部分
│   ├── Video SFT Data 🌿 ← 视频 + prompt + CoT + response 数据合成（Data Synthesis）
│   ├── World Model Data 🌱 ← 新方向：物理规律、因果、空间关系
│   └── Multimodal Quality Assurance 🌱
└── Agent Data 🌿 ← AiMe 观察建立：知识型问答数据 + 行为型使用轨迹两类（2026-08-24，见 AI 数据策略专题）
```

### 2.4 Data Strategy（数据策略）

```text
Strategy 🌿 ← 策略 PM 核心技能直接映射
│
├── Data Mixture 🌿 (黑盒 20% 配比实践)
├── Scaling Law 🌱
├── Active Learning 🌱
├── Data Ablation 🌱
├── Metric Design 🌱 ← 新增：指标定义是策略制定的起点
├── ROI Estimation 🌱 ← 新增：策略价值量化
├── 数据策略五问 🌱 ← 新增：采什么 / 从哪采 / 怎么用 / 怎么验证 / 优先级
│   （2026-08-24 重构：数据策略 = 五问决策链，不是流程描述）
│   ├── 数据来源组合（生产轨迹 / 人工标注 / 合成 / 用户纠正，按阶段配比）
│   ├── 数据消费路径分流（训练 / RAG / Skills-Tool 抽象，
│   │   分流标准 = 变化频率 × 需求性质 × 迭代成本 × 可控性）
│   └── 轨迹数据偏差（幸存者偏差：成功多失败少 + 对策：失败挖掘/
│       用户纠正/合成失败/主动采样）
└── Model Upgrade Adaptation 🌱 ← 基座换版的数据管线适配策略
    ├── 模板版本化（每个基座版本一套 prompt 模板）
    ├── 数据格式与模型解耦（CoT 规范独立于模型）
    ├── 分版本 Baseline（不跨版本比分数）
    └── 失配期 / 适配债：换版成本模型
```

---

## 三、Learning（学习机制）

```text
Learning
│
├── Deep Learning 🌱
│   ├── Transformer
│   ├── Attention Mechanism
│   └── Next-Token Prediction
│
├── SFT（监督微调）🌱 ← 靶向 SFT 三步闭环：轨迹对比（诊断）→ Rollout（造数）→ 模仿（2026-08-31）
│   ├── Loss Mask（loss 只在 response token）
│   ├── Teacher Forcing
│   └── 正样本质量 = SFT 天花板（↔ 质检标准）
│
├── Reinforcement Learning 🌱
│   ├── Policy Gradient
│   ├── PPO
│   ├── DPO
│   ├── GRPO
│   └── 拒绝采样（Rejection Sampling）🌱 ← 行为引导 Rollout = SFT 正样本合成，非 RL 参数更新
│
├── World Model 🌱
│   ├── State Representation
│   ├── Dynamics Prediction
│   └── Latent Space
│
├── Representation Learning 🌿 (InfoNCE+Triplet 八层解剖完成)
│   ├── Embedding
│   ├── Contrastive Learning 🌿 ← 难负样本 = 质量杠杆（美团消融 +11.02pp）
│   └── Manifold Learning
│
└── Optimization Theory 🌱
    ├── Loss Landscape
    ├── Grokking
    └── Phase Transition
```

---

## 四、Decision（决策系统）

```text
Decision
│
├── RL 🌱
│   ├── MDP
│   ├── Value Function
│   └── Policy Optimization
│
├── Recommendation 🌱
│   ├── Collaborative Filtering
│   ├── Two-Tower Model
│   └── Ranking
│
├── Search 🌿 (全景骨架建成：召回→排序→评测→闭环)
│   ├── Retrieval ← 召回层：词面(BM25) + 语义(Two-Tower) + 混合
│   ├── Reranking ← 排序层：LTR 三范式 + 语义特征注入
│   ├── Hybrid Search ← BM25 + 向量融合（RAG α=0.7 已实践）
│   ├── Ranking ← 精排漏斗：粗排→精排→混排 + 多目标
│   └── Query Understanding 🌱 ← 意图分类 / 改写 / 长尾分析
│
└── Planning 🌱
    ├── Tree Search
    ├── MCTS
    └── Hierarchical Planning
```

---

## 五、Evaluation（评测体系）

```text
Evaluation
│
├── 评测归因方法论 🌱 ← 覆盖子集/全量/野外集三层归因 + before/after 对照（2026-08-31）
│
├── 产品内验证与任务验收 🌱 ← 区分离线评测 / 运行时验证 / 交付验收；
│   质量与安全达标后看人工监督和返工成本（2026-09-10，见 daily/2026-09-10.md）
│
├── Judge Model 🌱
│   ├── LLM-as-Judge
│   ├── Pairwise Comparison
│   └── Rubric Design
│
├── Reward Model 🌱
│   ├── Bradley-Terry
│   ├── Preference Modeling
│   └── Reward Hacking
│
├── Benchmark Design 🌱
│   ├── Metric Selection
│   ├── Test Set Construction
│   └── Contamination Detection
├── Business Eval Set 🌱 ← 真实业务场景抽样评测集
│   ├── 换基座唯一决策依据（A/B：新旧基座在业务集上对比）
│   ├── 谁掌握业务评测集，谁有换基座否决权
│   └── Benchmark 分布偏差：通用榜 ≠ 业务子分布
│
├── Insight & Analytics 🌱
│   ├── Error Analysis
│   ├── Case Study
│   └── Pattern Discovery
│
└── A/B Testing 🌱
```

---

## 六、System（系统工程）

```text
System
│
├── Infrastructure 🌱
│   ├── GPU Cluster
│   ├── Distributed Training
│   └── Inference Serving
│
├── Data Pipeline 🌿
│   ├── ETL
│   ├── Stream Processing
│   ├── Batch Processing
│   ├── Async / Concurrency
│   ├── Queue System
│   ├── Monitoring
│   ├── Idempotency 🌱
│   ├── Checkpoint / Resume 🌱
│   ├── Dead Letter Queue 🌱
│   ├── Backpressure 🌱
│   └── Throughput & Bottleneck 🌱
│
├── Workflow Engine 🌿
│   ├── DAG Orchestration
│   ├── Retry & Backoff
│   ├── State Management
│   ├── Versioning
│   └── Error Handling
│
├── Agent Runtime 🌿
│   ├── Agentic Loop
│   ├── Harness 🌿 ← Agent 运行外壳：模型之外的一切（Claude Code / OpenClaw 即 harness 实例；八层解剖完成，见 notebook/Agent Harness 解剖.md）
│   │   ├── Agent Loop 工程实现（observe→reason→act→feedback→iterate）
│   │   ├── Tool System（工具注册 / 参数校验 / 执行 / 错误返回）
│   │   ├── Permission & Safety Boundary（文件系统/网络/命令授权）
│   │   ├── System Prompt & Scaffolding（行为指令层）
│   │   └── Environment Interface（终端 / 文件 / 浏览器 / GUI）
│   ├── Tool Calling 🌿 (LLM Repair = LLM-as-Tool)
│   ├── Context Management
│   ├── Memory System
│   ├── 任务委托与用户控制 🌱 ← AI-native 核心体验与 Agent 自主性分开分析；
│   │   持久任务状态 / 有界授权 / 暂停接管 / 人类注意力成本（2026-09-10）
│   ├── 入口-平台架构 🌿 (Agent 应用 = 入口层编排 + 业务平台能力层，价值随工具数增长；
│   │   AiMe = 极致形态：统一入口 + 深度集成 + 数据归集，2026-08-24)
│   └── 自主性分层 🌱 (输入/执行/编排三层；固定表单 = 输入层约束的产品决策)
│
├── MLOps 🌱
│   ├── Experiment Tracking
│   ├── Model Registry
│   └── CI/CD for ML
│
└── Serving 🌱
    ├── Model Deployment
    ├── Load Balancing
    └── Latency Optimization

├── Software Engineering 🌿
│   ├── Code Organization 🌿
│   │   ├── Monolith vs Pipeline Pattern
│   │   ├── Routes / Schemas / Services / Models 四层架构 🌿
│   │   └── File Splitting Strategy（按 Stage 拆，不按功能拆）
│   ├── Python 核心关键词（30词速查体系）🌿
│   │   ├── 基础骨架：import / def / class / return / if / for / try / f-string
│   │   ├── 数据容器：list / dict / None / True-False
│   │   ├── Models 层：db.Column / db.relationship / .add / .commit
│   │   ├── Services 层：.query.filter / raise / 类型标注
│   │   ├── Routes 层：@router / Depends / Query-Body-Path
│   │   └── Schemas 层：BaseModel / Field
│   ├── Debug 技能 🌱
│   │   ├── print() 调试法
│   │   ├── Traceback 阅读（从下往上）
│   │   └── NameError / TypeError / AttributeError 识别
│   ├── Code Review 技能 🌱
│   │   ├── 函数长度检查（> 50 行需拆分）
│   │   └── 函数命名规范（动词 + 名词）
│   └── 代码写作工作流 🌿
│       ├── 五步法：画图 → 数据样版 → 骨架 → 逐个填 → 跑通拆
│       ├── 从旧文件改 vs 新文件建的决策树
│       └── 改动面评估（PM 视角排期）
```

---

## 知识连接记录

> 记录每天新建立的连接。格式：`[日期] 节点A ← → 节点B：连接说明`

### 2026-07-10

**A. 学习系统初始化（10条）：**

- `Compressed CoT` ← → `Information Bottleneck`：压缩的本质是保留学习信号，丢弃冗余信息
- `Data Quality` ← → `Learnability`：数据质量的终极标准是"模型能不能从中学到东西"
- `Workflow` ← → `Data Pipeline`：Workflow 是 Pipeline 的控制层
- `Prompt Engineering` ← → `LLM Repair`：Prompt 是修复工具的接口设计
- `Schema Mapping` ← → `Transformation`：Schema 映射是数据转换的一种形式
- `Crowdsourcing` ← → `Sampling`：众包标注天然带有采样偏差问题
- `SFT Data` ← → `RL Data`：SFT 教模型"说什么"，RL 教模型"怎么想"
- `Python Concurrency` ← → `Data Pipeline`：并发是 Pipeline 吞吐的工程基础
- `Verification` ← → `Evaluation`：验证是评测体系的前置关卡
- `Post-training` ← → `Learnability`：后训练的效果上限由数据的可学习性决定

**B. 个人背景分析（8条）：**

- `Badcase 分类` ← → `Validation 反馈回路`：Badcase 处理本质是 Validation 的闭环
- `Function Calling` ← → `Agent Runtime 工具层`：FC 是 Agent 与外部系统交互的接口
- `Dify Workflow` ← → `DAG Orchestration`：Dify 可视化编排 = DAG 的有向无环图执行
- `LoRA 微调无 Evals` ← → `Evaluation 不可或缺`：没有评测的微调 = 没有方向的训练
- `Vibecoding` ← → `工程实现层`：自然语言→代码 = 当前工程能力的补充路径
- `Schema Mapping` ← → `Data Transformation 特化`：Schema 映射是数据转换的具体形式
- `Seed 模型数据供给` ← → `Data ↔ Learning 交界`：你在 Pipeline 和模型训练的接口处
- `三段经历的共性缺失` ← → `Evaluation 体系`：从第四范式到百度到字节，Evals 始终缺失

### 2026-07-11

- `CoT 质检` ← → `Learnability`：质检的终极标准不是格式对不对，而是压缩后模型还能不能学到
- `Rule + LLM + Human` ← → `Cascading Classification`：确定性层→概率层→人类层，你独立设计出了这个经典 ML 工程模式
- `黑盒 20%` ← → `Train/Test Split`：Eval Set 设计中的防过拟合策略，20% 是统计显著性与可分析性的平衡点
- `质检规则` ← → `Eval Set 有效性`：Garbage in, garbage out — 数据质量决定了评测的可靠性
- `学科标签纠错` ← → `Rule + LLM 双通道`：硬规则兜底覆盖确定性 case，LLM 覆盖模糊 case，人工仲裁分歧
- `Pipeline 修复` ← → `LLM-as-Tool`：将 LLM 作为修复工具嵌入 Pipeline，是 Agent 模式的工程化应用
- `众包 → 质检 → 修复` ← → `AI Data Pipeline 闭环`：完整的数据生产-质检-修复-交付链路
- `CoT 压缩` ← → `Information Bottleneck`：数学公式 min I(X;T) - β·I(T;Y)，压缩保留的是 Y 相关信息
- `Quality Gate` ← → `Cascading Classification`：多层关卡的每一层都在做分类决策：通过/修复/废弃
- `Eval Set 建设` ← → `Benchmark Design`：你正在实践的正是 ML 评测体系的基础设施建设
- `SQL + 数据分析` ← → `策略PM 生存技能`：不会取数跑数，数据和算法方向都走不远
- `A/B Test` ← → `Evaluation 体系`：实验设计是评测的工程化表达
- `策略PM 技能栈` ← → `AI Data 技能栈`：SQL/分析/实验/指标四件套完全通用
- `百度 Badcase 循环` ← → `策略迭代框架`：假设→实验→数据→迭代，同一个模式
- `字节 Pipeline 优化` ← → `策略指标提升`：合格率、修复成功率的优化 = 策略指标优化

### 2026-07-26/27

**A. Pipeline 深度解剖（非 CoT 专项，上升到系统设计范式）：**

- `Pipeline` ← → `DAG`：Pipeline 的数学基础是有向无环图，Stage = 顶点，数据流 = 边
- `Pipeline 质量` ← → `六大工程维度`：幂等性、断点续跑、错误隔离、可观测性、背压控制、状态管理
- `错误隔离` ← → `Dead Letter Queue`：坏数据不阻塞 Pipeline，进入死信队列单独处理
- `Pipeline 瓶颈` ← → `排队论 Bottleneck Principle`：整体吞吐量 = min(每个 Stage 的吞吐量)
- `Pipeline` ← → `Microservices`：同一思想在不同领域的应用——单元职责单一、接口契约明确、错误隔离
- `批处理 Pipeline` ← → `流式/微批/事件驱动 Pipeline`：四种工程形态，CoT 质检是批处理 + 事件驱动的混合
- `Monolith vs Pipeline` ← → `抽象原则（Abstraction）`：Pipeline 的本质是通过分解管理复杂度

**B. Pipeline 与 AI 全系统连接：**

- `Data Pipeline` ← → `Training Pipeline` ← → `Inference Pipeline`：Pipeline 贯穿 AI 系统全生命周期
- `数据质检规则有效性` ← → `训练 Loss 下降幅度`：质检的价值由模型训练效果验证
- `Quality Gate` ← → `Google TFX ExampleValidator`：工业界把数据校验作为 Pipeline 标准 Stage

**C. Python 代码与 Pipeline 的同构关系：**

- `def` ← → `Pipeline Stage`：每个函数 = Pipeline 的一个阶段，函数名 = Stage 名，参数 = 输入，return = 输出
- `Python 文件结构` ← → `Pipeline 结构`：routes/ = 输入层，services/ = 处理层，models/ = 持久化层
- `class` ← → `一组相关 def + 共享数据`：class 是更高一级的组织方式，封装相关操作和配置
- `Transformer 架构` ← → `嵌套 def Pipeline`：Attention 和 Feed Forward 都是 def，整个 Block 就是 def 调用 def
- `算子` ← → `def`：任何粒度的算子（原子操作/组合算子/Block）都是 def，差别只在内部数学复杂度

**D. 四层后端架构与 Python 关键词体系：**

- `routes/` ← → `装饰器 + HTTP 翻译`：@router.get/post 将 HTTP 请求映射到函数调用，不写业务逻辑
- `schemas/` ← → `Pydantic BaseModel + Field`：定义 API 输入输出格式，和 models 的区别是"校验 API 格式" vs "映射数据库表"
- `services/` ← → `纯 Python 业务逻辑`：全是 def，不引用 HTTP 框架，可被任何场景调用
- `models/` ← → `ORM 声明式映射`：class + db.Column() 描述表结构，不写复杂逻辑
- `db.Column / db.relationship / .query.filter` ← → `数据库操作基础词汇`
- `Depends / Query / Body / Path` ← → `FastAPI 参数注入机制`
- `print() / Traceback / NameError/TypeError/AttributeError` ← → `Debug 最小工具集`
- `函数长度 + 函数命名` ← → `Code Review 最快判断标准`

**E. Python 学习方法论：**

- `写代码五步法` ← → `Pipeline 设计思维`：纸上画图 → 数据样版 → 主流程骨架 → 逐个填 def → 跑通再拆
- `教学 Pipeline` ← → `金融认知锚点`：用交易清算、对账、风控审批等金融概念类比 Pipeline 各环节
- `改动面评估` ← → `PM 需求排期`：改逻辑 1-2 文件 → 加字段 3-5 文件 → 加模块 5+ 新文件 → 改架构 以月计
- `30 关键词速查卡` ← → `最小可用词汇量`：不学语法全书，只学覆盖 90% 场景的核心关键词

### 2026-07-28

**A. Anthropic Agent Eval 论文精读：**

- `Agent 评测` ← → `Eval System 三组件`：Task（任务）+ Trial（试验）+ Grader（评分器）= 完整 Eval
- `三种评分器` ← → `Cascading Classification`：代码评分器（确定性）→ LLM 评分器（灵活）→ 人工（黄金标准），与人质检的三层 Gate 同构
- `能力评测 vs 回归评测` ← → `CoT 质检规则演进`：新增规则 = 能力评测（攀登），稳定规则 = 回归评测（防守）
- `pass@k / pass^k` ← → `非确定性处理`：Agent 输出的随机性需要统计指标而非单次判断
- `评测驱动开发` ← → `自驱项目一批方法论`：8 步路线图（0:尽早开始 → 8:长期维护），直接指导项目一设计
- `评测饱和` ← → `CoT 质检规则迭代`：100% 通过的规则应毕业为回归测试，引入更难规则维持区分度
- `部分得分` ← → `三级分类体系`：qualified / needs_repair / discarded = 评测中不应只是二元通过/失败
- `评分针对成果不针对路径` ← → `Agent 评测核心原则`：Opus 4.5 "失败"但实际找到更好方案 = 评测规则不应僵化
- `Transcript 阅读` ← → `Debug 根因分析`：检查记录 = 区分"模型真的做错了"vs"评分器设计有问题"
- `Agent 评测` ← → `岗位 JD 精确对应`：DeepSeek 5 个 JD 中的评测/归因/数据管线要求全部覆盖

**F. 职业转折点记录：**

- `学科方向` ← → `视觉推理 + 世界模型`：组织调整，被动分配，从文本数据运营转向多模态数据运营
- `实习生话语权` ← → `专业判断能力`：话语权不靠职级争来，靠"能量化别人模糊描述的指标"挣来
- `组织变动` ← → `可带走资产`：Pipeline 认知框架 + 三视角思维 + 知识树 + 通用技能 = 不随组织变动而消失

**G. Pandas 结构化数据处理：**

- `DataFrame` ← → `Excel 表`：pandas 的基本操作单位，一张有行有列的表
- `pandas 操作` ← → `Pipeline Stage`：read_csv = 输入层，df[条件] = 过滤层，groupby = 分组层，to_excel = 输出层
- `质检规则` ← → `pandas 筛选表达式`：每条质检规则都有对应的 pandas 写法
- `df.groupby + agg` ← → `SQL GROUP BY`：分组统计的同构操作，pandas 和 SQL 一一对应
- `pd.merge` ← → `SQL JOIN`：表关联的同构操作
- `九大核心操作` ← → `日常 90% 需求覆盖`：read/explore/filter/groupby/sort/mutate/merge/dedup/write

### 2026-07-27（秋招备战）

**A. 目标明确：Agent 产品经理：**

- `职业目标` ← → `Agent 产品经理`：从 AI Data 方向升级为 Agent PM，保留数据策略作为核心竞争力
- `秋招` ← → `2026 年 8-10 月`：时间窗口紧迫，所有学习指向面试
- `目标公司` ← → `DeepSeek（幻方）/字节/月之暗面/智谱`：第一梯队 AI 公司
- `核心竞争力` ← → `数据 Pipeline 设计 + Learnability 理解 + 跨团队协作`：已有资产重新包装

**B. 岗位要求差距分析：**

- `5 个 DeepSeek 岗位` ← → `差距分析矩阵`：预训练数据团队/预训练数据工程师/通用Agent数据PM/Code Agent数据工程师，逐条对照
- `Python 编程` ← → `当前 3 分 → 目标 6 分`：能读代码 → 能独立写数据处理脚本
- `Agent 产品认知` ← → `当前 2 分 → 目标 7 分`：基础了解 → 深度使用+系统笔记+能讨论能力边界
- `简历/面试表达` ← → `当前 3 分 → 目标 7 分`：自然描述 → 子弹点公式+面试故事库
- `评测体系搭建` ← → `当前 3 分 → 目标 6 分`：人工质检 → 自动化多维度评测 Pipeline
- `模型训练理解` ← → `当前 3 分 → 目标 6 分`：Information Bottleneck → Pretrain/SFT/RL 数据需求差异

**C. 自驱项目计划：**

- `项目一：Agent 评测 Pipeline` ← → `预训练数据工程师岗位`：5 维度自动化评测 + 10 测试用例 + 分析报告 → 最核心项目
- `项目二：数据清洗 Pipeline` ← → `语言数据处理岗位`：MinHash 去重 + 质量过滤 + Metrics 统计 → 巩固基础
- `Agent 使用笔记` ← → `日常习惯`：每天记录 Claude Code 使用发现，积累到 30+ 条
- `每周检查清单` ← → `执行追踪`：Agent 认知/技术能力/项目推进/面试准备四维度自查

**D. 新文档体系建立：**

- `interview-prep/` ← → `面试准备目录`：gap-analysis.md + resume.md + story-bank.md + agent-insights.md + mock-interviews.md
- `论文阅读体系` ← → `Scaling Laws / Chinchilla / SWE-bench / MinHash`：7 篇核心论文，每篇只读 Abstract + Method 图 + Results
- `面试故事公式` ← → `[动作] + [方法] + [规模] + [结果]`：每条简历子弹点的标准格式
- `Pretrain vs SFT vs RL 数据需求` ← → `三段训练的数据质量标准差异`：去重→格式规范→偏好信号准确性

### 2026-08-03

**A. VLM 数据合成（新业务落地）：**

- `视频 + prompt + CoT + response` ← → `SFT Data`：一条完整 SFT 数据 = 输入（视频+prompt）+ 期望输出（CoT+response），instruction-following data
- `六类视频类型` ← → `VLM 能力短板`：计数/空间/行为/时间/实体/步骤 = 全部前沿模型的公认弱项（Fable 5 计数 3/10，BabyVision 不如 6 岁儿童）
- `CoT 生成` ← → `Learnability`：CoT 教"推理模式"而非"答案"，是跨场景泛化的关键
- `视频抽帧` ← → `Tokenization`：视频 → 视觉 token 的编码过程，帧密度 = 信息量，时间位置编码 = 视频独有的顺序信息
- `VLM 数据管线` ← → `CoT 质检 Pipeline`：方法论同构（原始数据 → 质量门控 → 合格数据），文本 → 视频迁移复用
- `数据合成（Data Synthesis）` ← → `数据侧核心业务`：自动生成 prompt/CoT/response ≠ 流程提效，是"喂给模型什么"的决策权

**B. 训练与评测链路：**

- `数据配比` ← → `Scaling Law`：数量 → 覆盖度，质量 → 信噪比，单条数据权重微乎其微但配比决定能力涨跌
- `评测回流` ← → `数据迭代闭环`：环节 6 的评测结果决定环节 1-3 采什么数据，闭环才是数据策略
- `SFT` ← → `数据质量`：teacher forcing 下每个 token 的 loss 都来自标注数据，错标直接污染训练
- `业务评测集` ← → `Benchmark 分布偏差`：通用榜是加权平均，模型换代是能力权重重新分布，A 涨 B 跌，业务依赖的 B 可能跌

**C. 基座模型 vs 业务适配（行业认知）：**

- `预训练目标` ← → `业务目标`：benchmark 分数（市场信号）vs 场景指标（业务信号），目标函数天然错位
- `能力上限` ← → `能力分布`：scaling 提升"上限" ≠ 业务依赖维度"变密"
- `模型换版` ← → `适配成本`：失配期（新基座+旧适配=行为漂移=短期回退）+ 适配债（永远追版本）+ 数据资产失效
- `模型无关数据管线` ← → `工程可复用性`：模板版本化 + 数据格式与模型解耦 = 数据资产跨版本复用
- `业务评测集` ← → `模型换版决策权`：谁掌握业务评测集，谁有换基座否决权

**D. 职业认知：**

- `量级 KPI` ← → `执行位 vs 设计位`：公司考核 ≠ 个人成长，主动做"质量验证"环节获得设计位证据
- `多模态数据` ← → `秋招差异化竞争力`：P2 加分项"了解多模态数据基础"直接落地，大多数候选人只有文本经验

**E. LLM 数据集全景：**

- `预训练语料` ← → `后训练数据`：学基础能力 vs 学行为模式，同一模型的两次投喂
- `训练期验证集` ← → `开发期验证集`：同名不同物，训练流水线仪表盘 vs 评测体系考试
- `评测集` ← → `Benchmark`：benchmark = 公开标准化的评测集子集；内部评测集保密防泄漏
- `训练集` ← → `评测集`：严格互斥，泄漏（contamination）= 分数失真
- `评测集构建` ← → `Eval Engineer 岗位`：任务定义/难度分层/防泄漏/指标设计/版本管理 = 独立岗位职责
- `质检 Gate` ← → `评测集`：微观（单条数据验证）→ 中观（Eval Set）→ 宏观（Benchmark）三层同构

### 2026-08-05

**A. VLM 多视频理解数据生产（新组工作落地）：**

- `L1/L2/L3 跨视频能力分层` ← → `Visual Reasoning 能力维度`：感知 → 对比 → 推理 = 跨视频理解的复杂度递进
- `可配对视频` ← → `关键词设计`：多视频数据采集的新约束——找"成对的视频"（同实体不同状态/同过程不同视角）而非单个视频
- `video_tos_link` ← → `数据持久化与治理`：TOS 入库引用 vs 网络 URL（失效/权限控制/可控性），交付引用而非原始链接
- `CoT 逐证据段落` ← → `跨视频推理`：多视频 CoT 采用"逐视频逐证据 + 汇总结论"结构，证据可追溯
- `标准文档拆解` ← → `Eval Set 任务定义`：能力维度定义 → 选材约束 → 交付格式 = 评测集构建的第一步
- `视频去重` ← → `Deduplication`：帧级/感知哈希去重 vs 文本 MinHash 去重，多模态去重难点
- `打码去信息干扰` ← → `Multimodal Quality Assurance`：数据治理中的隐私/信息干扰处理环节
- `飞书文档自动填写` ← → `数据交付自动化`：离线文档串联 = 交付自动化的落地形态

**B. Agent Harness 概念挂载：**

- `Harness` ← → `Agent Runtime`：Harness 是 Agent Runtime 的完整工程外壳（模型之外的一切），Claude Code / OpenClaw 都是具体 harness 实例
- `Harness` ← → `LLM Brain`：模型是大脑，harness 是身体——同一个模型换 harness 行为天差地别，产品差异的 80% 在 harness 层（Agent PM 的杠杆认知）
- `Harness` ← → `Tool Calling`：工具系统（注册/校验/执行/错误返回）是 harness 的核心组件
- `Harness` ← → `Claude Code 产品功能`：权限系统 / hooks / MCP / skills / worktree 隔离 = harness 层的产品设计决策
- `Harness` ← → `驾驭式使用`：深度使用的本质是感知并设计 harness 的约束（权限/工具边界/失败恢复），而非消费模型输出
- `Harness` ← → `Agent 评测`：评测应同时覆盖模型能力和 harness 行为（工具选择、错误恢复、权限边界），这是 Agent 评测与模型评测的分水岭

**C. 采集评测（Evals 落地 4，评测链闭环）：**

- `采集评测` ← → `Evals 方法论`：覆盖度矩阵/配对率/有效量 = Task + Grader 三组件在采集环节的落地（VLM 落地 4）
- `覆盖度矩阵` ← → `标准文档拆解`：标准拆解 = 目标定义，覆盖度矩阵 = 目标 vs 实际的差距可视化
- `可配对率` ← → `可配对视频`：配对率是「多视频数据可构造性」的量化指标（L2/L3 样本覆盖）
- `采集评测` ← → `CoT 质检` ← → `Agent 评测`：评测链三环节——单条门控（确定性高）vs 批次统计（确定性低）vs 维度化任务
- `采集评测` ← → `关键词策略迭代`：失败分布归因 → 补格/换词/去重前置，与 CoT 评估归因 → 修 prompt 同构

### 2026-08-20

**A. VLM 任务2 评分标准解剖（39-sheet 判分规范）：**

- `评测规范设计` ← → `LLM Judge 可靠性`：Deterministic Floor 的工业形态——单一事实型问题（比分/计数/速度）全部写成 0/1 规则判分，只有开放输出（字幕匹配）才用相似度阈值；"能写成规则的绝不留给 LLM"
- `评测规范设计` ← → `质检 Gate`：5 部件（任务对象/证据边界/输出格式/容错规则/排除项）= 评测侧"写死通过标准 + 排除项"，与 CoT 质检 5 层 Gate / A5 四层 rubric 同构
- `排除项设计` ← → `硬负例`：distractor（84拍 overlay、186 KPH 不算）+ 跨 topic 字幕排除 = 评测集最贵的资产是负例边界，防"答得像对但证据取错"
- `容差机制` ← → `判分一致性`：三类容差分治——文本（相似度 ≥ 0.9 容忍 OCR 噪声）/ 数值（±0.5 km/h）/ 时间（±3s），容差类型与问题类型配套
- `证据边界` ← → `歧义检测`：研发把"证据只能来自哪"写死（时间窗/字幕轨/画面对象），质检 L2 证据层可借鉴——不能只问"答案能否验证"，要问"证据来源是否唯一"
- `评测规范设计` ← → `训练数据格式规范`：accepted forms 写死 = 答案格式规范化的官方标准，即"答案按什么形态写，评分代码才能稳定判对"

**B. VLM 评测基准 + Reward Model（P0 缺口补齐，两份八层解剖落盘）：**

- `VLM 评测基准` ← → `Video Data Pipeline`：Video-MME 字幕轨设置 = 证据边界（↔ rubric 5 部件）；长视频掉分共性 = 数据价值洼地（↔ 难度坍缩：示例 1h vs 标注 10s）
- `VLM 评测基准` ← → `Benchmark Design`：基准有生命周期（MMMU 饱和 → MMMU-Pro 掉 17-27%），与 SWE-bench 污染失效同源
- `MMBench 判分管线` ← → `判分五模式`：规则匹配 → LLM fallback（91.5% 人机一致）= Deterministic Floor 的评测侧实现
- `Reward Model` ← → `评测规范设计`：reward function = 评测标准可执行化；BT 模型 = 用相对偏好信号学绝对打分函数
- `RLVR/GRPO` ← → `判分五模式`：能写成规则的绝不学函数——RLVR 规则 reward（DeepSeek-R1）与确定性判分同构
- `Reward hacking` ← → `质检 Gate`：Gate gaming（标注者反向利用通过标准）= 训练侧 reward hacking，同构于 Goodhart 定律
- `Reward hacking` ← → `Contamination Detection`：模型 hack 评测 ↔ 评测集泄漏，同构风险（METR：agentic 任务 hacking 率 25-100%）
- `Bradley-Terry` ← → `人人一致/人机一致`：偏好一致性与 judge 一致性是同一假设前提

### 2026-08-21

**A. 美团搜索 3.0：LLM 语义表征三期实践（papers/07，与 price-agent 对标）：**

- `Representation Learning` ← → `搜索排序`：LLM 语义表征作为精排特征的三期工程路径——验证可行性（64 维分桶注入）→ 系统性升级（对比学习）→ 跨场景迁移（覆盖率修复）
- `Contrastive Learning` ← → `难负样本`：InfoNCE（batch 内负样本）+ Triplet（同请求同商家曝光未点击）双损失；消融 Q2I-Order-AUC +11.02pp > Click-AUC +4.85pp——"从分类到排序"的转型证据
- `Embedding` ← → `Prompt Engineering`：反直觉——Embedding 训练场景 Prompt 做减法（精简信息陈述+总结引导 > 复杂任务指令），Prompt 作用是"聚合哪些信息"而非"完成什么任务"
- `Evaluation` ← → `长尾指标`：长尾 NDCG@5 +2.21pp / BadCase@1 -2.96pp——语义提升在长尾场景体感最明显（词面匹配最薄弱处）
- `RL Data` ← → `行为标签监督`：点击/下单行为分布（1:3:6）作为表征训练监督信号，与 LLM Judge 监督形成对比（行为信号客观可规模化）
- `Data Strategy` ← → `覆盖率思维`：表征迁移第一道关卡是 Query 覆盖率（81.24%→98.92%），"复用模型"≠"可直接上线"；特征有效性前置验证（cosine 单调性与 label 正相关）
- `评测→训练闭环` ← → `Producer→Consumer 闭环`：把下游排序相关性作为蒸馏信号回灌表征训练 = 06 号笔记闭环思想在表征场景落地

**B. 搜索系统全景骨架（notebook/搜索系统全景，Search 节点升级 🌿）：**

- `Search` ← → `Agent`：搜索是 Agent 的工业前身——RAG 检索=召回层、意图路由=Query 理解、工具选择=重排、多轮上下文=Query 改写
- `Search` ← → `price-agent`：price-agent 已覆盖召回层 + Query 理解（MVP 版），M4 Rerank 落地 = 补上排序层，凑齐三层骨架
- `Search` ← → `Evaluation`：搜索评测 = 离线（AUC/NDCG/GAUC/长尾）+ 在线（AB/AA/漏斗指标）双体系，是 Agent 评测方法论的先导
- `Search` ← → `CoT 质检`：搜索数据闭环（曝光日志→样本→特征→模型）与 CoT 质检 Pipeline（流入→门控→交付）同构——"通过标准定义 + 归因闭环"是同一思维
- `Search` ← → `LTR`：美团一期→二期升级轨迹（BCE→对比学习）= LTR 从 pointwise 到 pairwise 的演进缩影
- `Agent 评测` ← → `搜索评测双体系`：离线（AUC/NDCG/消融）+ 在线（AB/AA/漏斗）双层评测可移植——"离线涨 ≠ 真实好用"，Agent 上线需小流量 AB 阶段（notebook/搜索工程到Agent的迁移地图）
- `Agent 评测` ← → `指标归因`：过程 vs 结果指标——QV 降 QV_CTR 升 = 曝光质量优化；Agent 里工具调用降但完成率升同理
- `RL Data` ← → `难负样本`：Agent 轨迹"低效路径"= 搜索"曝光未点击"同构——从真实行为挖训练信号
- `Harness` ← → `信号注入`：拼接→分桶→门控（PEPNet +25bp）演进 = 模型能力进系统的接口设计范式
- `Agent 评测` ← → `AA 校验`：行为方差大的系统先验无偏再读实验差异

### 2026-08-23

**A. VLM 训练演进与数据难度升级（notebook/VLM训练演进与数据难度升级）：**

- `Multi-modal Data` ← → `VLM 训练演进`：四阶段演进（图文对齐→统一生成式→视频起步→长视频复杂推理），数据难度 = 能力演进顶层的信号
- `Visual Reasoning` ← → `证据锚定`：六类能力短板（时间交叉引用/实体匹配）正是 2026 数据构造主战场（LongVT/VideoTemp-o3 的 locate-crop-answer 范式）
- `Video SFT Data` ← → `三阶段闭环`：Cold-start SFT → Agentic RL → RFT（LongVT 范式）——我们的数据未来消费方式可能是 RL 而非纯 SFT
- `Video Data Pipeline` ← → `反捷径过滤`：质检"证据边界"= 数据构造 video-dependency 过滤（VideoKR 三重过滤：self-consistency/video-dependency/CoT）
- `Multimodal Quality Assurance` ← → `跨视频推理空白`：2026 公开文献 multi-video reasoning = open gap——我们的多视频数据生产在补行业空白
- `RL Data` ← → `联合奖励`：answer 正确 + 时间定位（IoU-aware）防 reward hacking——与判分规范的时间容差部件同构

**B. AI 策略产品与评测能力地图（notebook/AI策略产品与评测能力地图，职业定位主线）：**

- `评测四层模型` ← → `AI 策略产品`：L2/L3（体系设计 + 数据生产）是策略产品的基础能力，L4（驱动训练闭环）是天花板；L1 执行可被自动化替代
- `评测 × 数据生产` ← → `差异化壁垒`：做评测的不懂数据生产、做数据的不懂评测设计——"为评测构建数据 + 用评测结果驱动数据策略"交叉点上人很少
- `评测驱动迭代` ← → `数据飞轮`：评测 → bad case 归因 → 数据策略 → 训练迭代 = 策略产品的核心工作方式
- `Reward Model` ← → `评测的极端形态`：评测标准可执行化 = 评测从"验证模型"变成"训练的一部分"（L4 增值的依据）
- `评测方法论` ← → `领域迁移`：基准构建/统计验证/防污染/Judge 可靠性跨领域可迁移；物理世界模型/VLA/harness 评测值得关注不现在深入
- `评测体系搭建` ← → `gap-analysis 优先级`：定位升级 P1 → P0（2026-08-23）

**C. 职业目标收敛（2026-08-23 决策）：**

- `评测驱动策略产品` ← → `职业目标收敛`：主攻评测驱动的 Agent/策略产品岗，预训练数据工程师降级备选，Code Agent 数据工程师短期放弃
- `岗位选择` ← → `故事主线唯一`：秋招窗口有限，故事只能讲一个主线——评测 × 数据交叉（产品岗最能变现）

**D. 算法反馈迭代闭环（notebook/算法反馈迭代 + projects/vlm-eval-feedback-loop，短板补齐）：**

- `评测结果反馈` ← → `数据飞轮`：评测 → bad case 归因 → 数据策略 → 训练迭代，闭环的最后一环
- `判分规范` ← → `reward 设计`：判分五模式 = 规则 reward（RLVR）的评测侧实现——评测设计好的人天然理解 RL 数据需求
- `质检` ← → `SFT loss 护栏`：teacher forcing 下错标直接污染 loss，质检是 loss 质量的护栏
- `bad case 根因分类` ← → `数据策略映射`：四类根因（任务定义/数据源/标注执行/评测规范）各对应一类数据策略动作
- `单变量验证` ← → `实验设计`：一次只改一个变量，涨了归因明确跌了回滚明确；同评测集新旧对比 + 护栏指标（防难度坍缩）

**E. VLM 任务2 评测脆弱点归因（notebook/vlm任务2评分标准rubric整理分析.md 第十章）：**

- `判分代码` ← → `评分规范`：代码与规范 gap 造成"答对但判错"假失败（数字转写缺失/多数字取第一个/时间格式不全）——先修评测规范误伤再归因数据，否则归因全被污染
- `前置归因` ← → `实证归因`：无判分结果时从规则结构反推失败风险点（评测规范问题/任务定义问题），拿到数据后按四类根因补充
- `模糊 rubric` ← → `Deterministic Floor`：M099/M101 开放判断 → 依赖 LLM judge 引入偏差；拆解为可判子项是收口路径
- `计数/时间定位失败` ← → `模型能力短板`：行业共性（Video-MME 长视频、Fable 5 计数 3/10、LongVT 时间交叉引用），此类归因走数据难度分级而非改评测

**F. 公开案例佐证评测脆弱点（rubric 文档 10.6，行业验证）：**

- `解析器假失败` ← → `判分代码 gap`：verl/lm-eval-harness 案例证明解析器差异让同一模型 0→0.95、1/5 正确回答被格式误判——"先修评测规范"是消除 12%+ 级系统误差，非吹毛求疵
- `格式合规` ← → `能力评测`：格式是 gate 不是 reward（311 倍格式效应 + "格式奖励 1.0 但准确率 0"警示）→ accepted forms 收口不改成"格式好就得高分"
- `计数短板` ← → `解码瓶颈`：BLINK 人类 93.75% vs 开源 10.83%；线性探针证明视觉信息足够但 LM 解码失败 → 数据难度分级 + RL 信号比盲目加量对症
- `长视频数据` ← → `长视频能力`：VISTA 长视频数据微调 +3.7（Video-MME），加短视频无增益——数据难度 = 能力演进顶层的信号

**G. 百度项目反刍：入口-平台架构、三层评测与自主性分层（projects/baidu-agent-migration 重构）：**

- `入口-平台架构` ← → `Agent Runtime`：Agent 应用 = 入口层（意图识别 + 任务编排）+ N 个业务平台（各自功能与链路）；价值在编排层，随工具数增长——一个工具时像"功能"，N 个工具时编排本身才是 Agent
- `入口-平台架构` ← → `MCP`：平台标准接入 = MCP 解决的 N×M 集成问题；百度品牌 MCP = 资产库对模型的标准接入层（AI 品专验证）
- `固定输入表单` ← → `Harness`：表单 = 产品层的输入侧约束（Schema 的人机界面），从根上消除 query 理解错误——约束输入空间 = 缩小决策空间
- `三层评测` ← → `Agent 评测`：平台断言校验 / 入口 query 评测集 / 编排任务级评测（τ-bench 思路：tool selection + 任务成功率）——评测必须对应架构分层，否则跨层归因无解
- `三层评测` ← → `Metric Design`：92% 只是平台层指标 ≠ Agent 指标；Agent 的指标 = 端到端任务成功率（入口 + 编排层）
- `自主性分层` ← → `输入约束`：输入/执行/编排三层自主性；放开一层自主性的前置条件 = 多一层评测兜底
- `自主性三阶段演进` ← → `三层评测`：表单链路 → 直接 query（前置：query 评测集）→ 多任务复杂 query（前置：任务级评测），迁移融入营销 Agent 成为工具集一个节点

### 2026-08-24

**A. AI 数据策略框架（notebook/AI 数据策略：从 AiMe 到 B 端 Agent 平台）：**

- `统一入口` ← → `数据归集位置`：入口的战略价值不只是使用成本，更是所有平台使用轨迹的汇聚点——谁在入口谁有数据，谁的 Agent 迭代最快
- `Agent Data` ← → `入口-平台架构`：AiMe = 入口-平台架构极致形态；统一入口让轨迹数据天然集中，架构即数据管线
- `使用轨迹数据` ← → `Skills/Tool 定义`：轨迹可蒸馏为 skills（操作知识编码）——"人的使用说明书 → Agent 的说明书"
- `数据消费路径分流` ← → `数据策略`：不是所有数据都要训练——变化快走 RAG、流程固定抽象 Skills、能力缺口才训练（分流标准：变化频率×需求性质×迭代成本×可控性）
- `轨迹数据` ← → `幸存者偏差`：生产轨迹成功多失败少（失败即中止），直接训练 = 只见成功不见失败；人工纠正数据 = 最便宜的偏好对
- `Agent Data` ← → `Harness`：harness 可观测性（每步 tool call 日志）= 轨迹数据采集地基；harness 同时是 RL rollout 环境
- `问答评测 + Agent 使用评测` ← → `评测四层模型`：两套评测对应两类迭代——问答评测驱动 RAG/微调，使用评测驱动 Skills/Tool/训练；B 端 ground truth 稀缺 → 行为信号（完成/重试/接管）兜底
- `B 端数据壁垒` ← → `客户使用轨迹`：模型同质化时代，轨迹数据积累 = 平台 moat；B 端难点从"数据量"变成"数据回流（授权+脱敏+合规）"
- `AiMe 观察` ← → `price-agent`：自驱项目 = B 端"入口+工具+评测"最小实践场，缺的是轨迹数据和评测闭环
- `Agent Data` ← → `算法反馈迭代`：评测→归因→数据策略闭环在 Agent 平台场景的映射（问答/使用两条闭环）

**B. v2 重构：流程层 → 数据策略层（同日第二稿）：**

- `数据策略五问` ← → `能力地图`：数据需求是能力需求的镜像——先定能力再定数据规格，不是有什么采什么
- `数据来源组合` ← → `采集方案`：四来源（生产轨迹/标注/合成/用户纠正）按真实性×定向性×成本权衡，组合随阶段演进；用户纠正数据 = Agent 平台特有红利
- `评测` ← → `数据投资回报`：评测不是验收环节，是数据投资的回报度量——ROI 语言对齐算法预算（连接 ROI Estimation）
- `流程层 vs 数据策略层` ← → `讲述结构`：流程回答"系统怎么跑"，数据策略回答"系统凭什么越来越强"——面试表述必须落在决策链，架构只是生产系统

**C. Next Level 思考（市面平台能力收敛 → 四个升级向量）：**

- `Next Level` ← → `自主性分层`：自主性每放开一层，评测兜底深一层——任务级→绩效级→治理级评测（百度反刍的直接升级版）
- `数据飞轮闭合` ← → `轨迹蒸馏`：next level 的引擎——飞轮上的人工环节（写 skills/归因/采数）自动化，谁先闭合谁先自驱进化
- `市场治理 + 绩效评测` ← → `评测四层模型`：next level 平台的核心岗位仍是评测×数据交叉点（L4 驱动训练闭环的升级形态）

### 2026-08-30

**小红书帖子精读（08-12 五篇，见 papers/）：**

- `算法岗位定义/技术杠杆` ← → `数据投资回报`：价值的终极标尺都是"改变产品/公司竞争力"，而非省时/省卡（08）
- `选问题/Research Taste` ← → `Evaluation 职业主线`：评测真正稀缺的是"什么问题算难、算真"的判断力（08/09 共点）
- `Bench/评测判断力` ← → `Evaluation`：通用榜饱和，稀缺的是领域判断力；跨界背景吃香（09）
- `Agentic Data` ← → `Agent Data`：数据合成底座指数级扩张，环境组已超建模组（09）
- `reward hacking` ← → `Reward Model`：不遵循 bitter lesson / 只优化局部信号即为 reward hacking（09）
- `蒸馏 loop` ← → `Frontier`：跟随式学习短期最快，追到 frontier 才发现缺独立解难题能力（09）
- `系统视角` ← → `System`：局部指标涨 ≠ 系统变好，避免给下游挖坑（09）
- `冷启动评测集` ← → `Evaluation`：无线上数据路径，能力地图 + 金种子 + 受控变异（10）
- `三层 Oracle` ← → `LLM Judge 可靠性`：程序 > 规则 > Judge > 人工，Judge 只看证据、可 unknown（10）
- `六数据桶` ← → `Benchmark Design`：Gold/Coverage/Challenge/Regression/Canary/Hidden 数据产品化（10）
- `可重置环境` ← → `环境/状态隔离`：fixture 是 case 定义的一部分，防状态污染与泄漏（10）
- `Guardrail vs Optimization` ← → `评测四层模型`：安全门禁独立于成功率，禁止加权（10）
- `Task/Trial/Outcome/Grader` ← → `Agent 评测`：评测系统五件套，先懂零件再学指标（11）
- `Agent Harness vs Evaluation Harness` ← → `Agent Harness`：测的是模型+harness 系统，非裸模型（11）
- `Harbor Task Structure` ← → `Benchmark Design`：一道 Task = 完整实验包（说明/环境/参考解/测试/运行配置）（11）
- `Public vs Business Benchmark` ← → `Evaluation`：双层评测，通用坐标 + 业务适配，互补非替代（12）
- `Quality/Cost/Time` ← → `模型选型 / 数据投资回报`：选型是三维权衡，成本语言对齐算法预算（12）
- `评测资产化` ← → `Benchmark Design`：任务集/评价标准/失败样本是长期积累的基础设施（12）

### 2026-09-01

**A. VLM 靶向 SFT 消费链路复盘（projects/vlm-sft-pipeline-review + daily/2026-08-31）：**

- `靶向 SFT 三步闭环` ← → `SFT / 数据飞轮`：轨迹对比（诊断）→ 行为引导 Rollout（造数）→ SFT（模仿）；交付的三元组是闭环原料，覆盖子集 +2.8 是数据价值的凭证
- `拒绝采样` ← → `RLHF / DPO / GRPO`：Rollout 产物进 SFT 训练集 ≠ RL 参数更新；区别在"是否在线采样 + 优化目标"
- `评测归因方法论` ← → `before/after 对照`：覆盖子集 +2.8（学会没）/ 全量 +0.5（被 89 题未覆盖稀释）/ WildClaw +2.3（泛化没）三层归因；两轮唯一变量 = 模型权重
- `质检标准` ← → `SFT 正样本质量定义`：交付层 ↔ 消费层握手协议——内容准确性→标签噪声 / 回答完整性→过程缺陷 / 格式规范→格式漂移 / 视频匹配度→语言捷径
- `考点先行寻源` ← → `难度坍缩`：难度对齐验收前移到采集 = 难度坍缩源头治理；采集第一道难度防线，质检最后一道
- `分题型 pipeline` ← → `能力维度 L1/L2/L3`：题型即考点即能力维度
- `SFT` ← → `数据质量`：正样本质量 = SFT 天花板（不显式压低坏样本）；epoch 1-2 + 混通用数据防翻车；loss 只在 response token

### 2026-09-02

**A. 三类 PM 分层框架（与上游 AI 数据与安全 PM talk，daily/2026-09-02）：**

- `三类 PM 分层` ← → `优化对象离模型参数的距离`：传统 PM 改产品逻辑 / AI 应用 PM 改模型调用方式（harness）/ AI 策略 PM 改模型参数（经由数据）——由远及近
- `AI 应用 PM vs AI 策略 PM` ← → `Harness 层 vs 参数层`：前者杠杆在 harness（同模型换 prompt/工具/编排），后者杠杆在参数（数据→权重，突破 harness 天花板）；百度 92% = harness 收敛极限，是这条分界线的实证
- `数据角色质变` ← → `决策依据 vs 生产资料`：传统 PM 用数据做决策、AI 应用 PM 用 case 调 harness、AI 策略 PM 把数据当训练生产资料——这是真·策略 PM 的试金石
- `AI 策略 PM 工作方式` ← → `实验科学 / Research Taste`：idea→假设→实验→造数据→训练→评测→归因→迭代；选对问题 > 解问题（↔ papers/08 算法岗 = Applied Research Engineer）
- `以 benchmark/SOTA 为目标` ← → `Goodhart / reward hacking / 业务分布偏差`：benchmark 是能力的可观测代理信号，不是目的；懂评测的人知道它何时失真（榜饱和 / 数据污染 / 通用榜≠业务分布）——评测×数据交叉点最值钱处
- `三类 PM 外部校准` ← → `gap-analysis 职业定位`：上游独立给出的第三类 = 我收敛的主线，talk 是外部验证

**B. AI 数据策略 PM 工作闭环 + 数据构造/合成补课（notebook/AI 数据策略 PM 工作闭环）：**

- `数据策略 PM 工作驱动` ← → `能力 gap / 评测`：驱动 = 目标能力−当前能力；让 gap 可见可行动的仪器 = 评测（既是起点暴露问题、又是终点验证效果）
- `发现改进点` ← → `维度化评测 + 错误环节分层`：错在感知层→加视觉/证据数据、推理层→加 CoT/过程监督、格式层→改评测规范（不加数据）；先修评测误伤再归因数据
- `idea 来源` ← → `bad case > 论文`：一手 bad case = 没人解的真问题（护城河），论文 = 别人验证过的解；陷阱 = 蒸馏 loop / reward hacking（↔ papers/08+09）
- `落实执行纪律` ← → `小批验证 / 单变量 / 防泄漏 / 护栏指标`：靶向 SFT 三步闭环是落地实例
- `数据构造 ⊃ 数据合成`（新节点 🌿，升级 Synthetic Data 🌱）：分辨轴 = signal 是采来的还是生成的；合成四谱系（模型/程序规则/仿真渲染/环境交互）
- `合成命门 = verifier` ← → `拒绝采样筛选 = 质检 Gate / LLM Judge 可靠性`：可验证任务程序判分（RLVR）、开放任务 judge/人工；合成五风险（幻觉/分布失真/模型坍缩/多样性不足/反捷径泄漏）
- `数据质量+规模可换参数量` ← → `papers/06`：32B+5k 高质量合成打败 235B 未微调 = 数据策略 PM 价值的最硬证据
- `我的经历映射` ← → `构造（VLM 寻源/Trace→SFT）+ 合成（Rollout 拒绝采样/LLM Repair）`；待补 = 渲染/扩散 + 程序化合成（gap-analysis D2/D3）
- `测试模型能力边界`（新节点 🌱）← → `能力评测 / 难度探测`：主动"压"模型找崩点（难度升级/OOD/组合/对抗四类探测）；边界 = 能力 gap 的精确形状 = 下一步造什么数据的依据
- `公开 benchmark 探边界三陷阱` ← → `污染 / 通用榜≠业务 / 得分低≠缺陷`：真实边界 = 公开榜 + 自建困难集 + 业务评测集 + 归因；防 Goodhart（对着 benchmark 训 = 刷榜）
- `探索→放量二阶段` ← → `MVP-then-scale / 渐进式投资`：小批（PM+算法验证方向）→ 大批（下游生产）；小批验证的是方向有效性不是终值
- `放量三陷阱` ← → `非线性放大 / 小批过拟合 / 灾难性遗忘`：★VLM 覆盖子集 +2.8 被稀释到全量 +0.5 = 非线性活案例；放量瓶颈是覆盖率不是质量

**C. 被动补洞 vs 主动抬天花板：模型能力主动提升（notebook/AI 数据策略 PM 工作闭环 第九节）：**

- `被动补洞 vs 主动抬天花板`（新节点 🌱）：被动的不是「评测驱动」而是「回看型评测」；主动 = 前瞻押注杠杆，同样被 ablation/scaling curve/前沿探针测——评测既是补洞探测器也是抬天花板指南针
- `主动进步五杠杆` ← → `算力规模 / 架构 / 训练范式 / 数据范式 / 推理时计算`（+ 元杠杆目标函数）：数据范式是我能亲手动的杠杆，其余理解到「动哪个方向」层
- `读论文姿势` ← → `五杠杆地图 / 精读方法论方法层`：系统≠全读，系统=按杠杆归位每篇；纯跟论文=蒸馏 loop（papers/09）
- `理论→行动链路` ← → `小实验验证机制→ablation→scaling→集成`：与「小批验证→放量」同构，发生在研究层
- `PM 四主动杠杆` ← → `前瞻能力地图 / 需求→能力规格 / authoring 新评测 / 数据范式创新`：PM 动「往哪抬」（定义目标），算法动「怎么抬」；选对方向 > 解问题（papers/08）
- `authoring 新评测` ← → `定义新北极星`：主动造还没人做的能力评测 = 把评测驱动从被动补洞变主动抬天花板（↔ papers/03/10/12 真实任务评测蓝海）

### 2026-09-03

**AgentLoop 数据飞轮实践系列精读（papers/13，共 5 篇 · 已完结）：**

- `数据飞轮`（新节点 🌳）← → `评估框架`：接入→观测→审计→数据集→评估→实验→经验库 七环节首尾相接；评估产出是实验输入，实验结论指回下一轮评估——数据策略 PM 工作闭环的平台级实例
- `评估器 vs 评估任务`（新节点 🌿）← → `质检规则 vs 质检任务`：考卷+评分标准（可复用资产）vs 组织一场考试（执行配置）；与 CoT 质检项目的"质量标准定义"↔"批量质检"完全同构
- `Rubric`（新节点 🌿）← → `SFT 正样本质量定义`：把专家的隐性判断固化为可判定细则（0-4 分档+权重+总分）；Rubric 写得越具体，打分越稳定越可解释；交付层↔消费层握手协议的另一表述
- `题目级 Rubric`（新节点 🌿）← → `评测粒度`：线上通用标准 vs 实验 per-case 标准；题目粒度绑定自己的 Rubric（schema 加列→评估器按传入评估→实验读取），哪题低分补哪题能力点
- `评分证据/可解释分数` ← → `评测归因方法论`：decision/explanation/evidence + rubric_version = 分数变化归因的前提（区分 Agent 变了 vs 标准变了）；"先查尺子再看东西" = eval attribution 的操作版
- `离线实验平台`（新节点 🌿）← → `数据治理`：Agent 数据不出内网——内网执行引擎 + 云端大盘；最小权限 AK（专用 FullAccess 身份）+ region/AgentSpace 绑定
- `实验回测`（新节点 🌿）← → `A/B 实验设计`：数据集+评估器+变量映射+版本号；定时调度回归（生产每天）、窗口均值消除浮动、消融实验验证经验注入（耗时 -30~40% / 成本 -20~47%）
- `轨迹 Trajectory vs Trace`（新节点 🌿）← → `Agent 可观测性 / OTel`：Trace=微服务视角，Trajectory=智能体行为视角（prompt+每步+思考+输出）；评 Agent 执行过程必须用轨迹数据
- `评估采样降本` ← → `小批验证 / 渐进式投资`：Agent 评估一条贵 → 采样（100 条）先跑通再放开；与"小批验证方向→放量"同构（评测执行的成本纪律）
- `接入方式四选型`（新节点 🌿）← → `Agent 形态 × 改造意愿`：通用 Agent 一键 / 框架 SDK / 高代码注解 / eBPF 无侵入——探针覆盖前三类，eBPF 兜底，按 Agent 形态与改造意愿选成本最低的路
- `papers/13` ← → `papers/06 eval-to-training-loop`：机制设计（SWE-smith）↔ 平台落地（AgentLoop）；`← → papers/03/10/11/12` real-world eval 系列：真实场景评测的工程化操作全集

### 2026-09-04

**pass@k 数据筛选五缺陷精读（papers/14，小红书「小red同学」）：**

- `pass@k 数据筛选`（新节点 🌿）← → `AI 数据策略 PM 工作闭环`：本文是"评测暴露 gap → 归因 → 定向造数据"闭环在 RL 数据筛选环节的独立实证；pass@k = 用"模型当前成功率"当难度代理，贵/漂移/有噪声
- `pass@k 五缺陷` ← → `Goodhart / 代理目标`：算力浪费+难度漂移+reward 噪声+自我循环+目标错位——五点本质都是"优化代理信号而非真目标"
- `模型弱点驱动数据构造` ← → `数据构造 vs 合成`：benchmark 失败样本→能力归因→定向构造同分布数据→可验证低噪声 reward→定向 RL；"只分析新失败样本，不重筛通用池"把成本从 O(全量×k) 降到 O(失败样本)
- `reward 噪声污染难度估计` ← → `LLM Judge 可靠性（papers/05）` + `CoT 质检 AA 校验`：judge 一致性差 → "成功率居中"可能只是抖动而非能力边界；用 Kappa/AA 校验控 judge 可靠性是前提
- `训练分布≠benchmark 目标能力` ← → `Warren Optima（papers/12）通用榜≠业务分布`：pass@k 目标错位 = 同一命题的 RL 版本
- `pass@3 − pass@1 gap` ← → `能力 vs 稳定性判别`：gap≈10pt 时定向 RL 约可涨 16pt；gap 大 = 有能力但不稳定 = RL 有空间（决策信号）

### 2026-09-10

**Agent 产品认知与 vlm-agent 数据闭环（[当日记录](../daily/2026-09-10.md)，按主题合并）：**

- `学习范围` ← → `vlm-agent 项目`：优先弄懂数据获取/构造、Rollout + SFT、评测与失败反馈，按需补算法基础，不深入预训练全栈。
- `AI-native / Agent 自主性` ← → `有界任务委托`：区分核心体验与执行权限；Harness 提供工具、状态、反馈和接管，产品质量还需看监督与返工成本。
- `成熟技术` ← → `真实任务卡点`：从上下文、可靠数据和后续动作寻找应用机会，不预设全自主系统或把场景假设当成市场事实。
- `Rubric 演进` ← → `人工决策 / 版本管理`：人定标准与仲裁，Agent 执行和提疑点；独立验收后逐步自动化，一致样本也需抽检。
- `质检错误` ← → `学习信号 / 边际数据价值`：错放可能引入错误监督，错杀可能损失覆盖；高价值不等于高难度，影响取决于替代性与训练暴露。
- `正确答案 / 错误 CoT` ← → `证据忠实性`：答案正确不抵消错误过程监督；证据错误、逻辑错误与额外成本需分别判断。
- `能力边界` ← → `错误归因 / 数据策略`：区分输入缺证据、模型能力不足、工具失败与评测误判，再决定补数据还是修系统。
- `高质量数据 / Scaling` ← → `未见任务泛化`：靶向数据与规模扩展不对立，“VLM scaling 失败”仍待具体证据；loss 下降不等于能力提升。
- `项目收益` ← → `消融隔离`：三套独立基准分开解释，多数据线共享基座下的 Agent 涨分不等于本项目净贡献。
